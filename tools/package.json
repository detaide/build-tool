{
  "name": "tools",
  "version": "1.0.0",
  "description": "",
  "main": "watcher.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bversion" : "./node_modules/.bin/esbuild --version",
    "build" : "esbuild ./setup.mjs --bundle  --platform=node --target=node18.16"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.21.4"
  }
}
